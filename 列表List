__author__ = "billpower"
__version__ = "2019.12.24"

import rhinoscriptsyntax as rs

dpoints = List  #定义来源数据
dpointx = list(range(len(dpoints))) #建立数据索引列表

DataOut = List  #输出源数据
dpointx = dpointx   #输出索引

print(dpointx)  #输出数据检查
print(dpoints) 
